interface:
  display_name: "Astra Orchestrate"
  short_description: "Route workers by task and resume on completion"
  default_prompt: "Use $astra-orchestrate to plan this task, select workers by scope and difficulty, and resume on completion through the runtime's supported event mechanism."
